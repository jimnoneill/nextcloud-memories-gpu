# Nextcloud Memories GPU Transcoding
# Copy to .env and configure

# =============================================================================
# Required
# =============================================================================

# Storage server (where Nextcloud runs)
STORAGE_IP=192.168.1.10

# GPU server (where go-vod runs)  
GPU_IP=192.168.1.20

# Path to Nextcloud data directory on storage server
NEXTCLOUD_DATA=/mnt/storage/nextcloud/data

# Your Nextcloud domain
DOMAIN=cloud.example.com

# =============================================================================
# Optional
# =============================================================================

# go-vod port (default: 47788)
GO_VOD_PORT=47788

# Nextcloud Docker container name (default: nextcloud)
NEXTCLOUD_CONTAINER=nextcloud

# NFS mount point on GPU server (default: /mnt/nextcloud-data)
NFS_MOUNT=/mnt/nextcloud-data

# Transcoding quality factor: 15-45, lower = better (default: 28)
QUALITY=28

# Request timeout in seconds (default: 300)
TIMEOUT=300
